{
  "mcpServers": {
    "codegraph": {
      "command": "dotnet",
      "args": [
        "run",
        "--project",
        "/absolute/path/to/src/CodeGraphMcp",
        "--",
        "${workspaceFolder}"
      ]
    }
  }
}
